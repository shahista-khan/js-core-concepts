//it compares string on the basis of ascii values for eg pen>book
//if we campare a string to a number it will convert it to a number and then compare
//== checks only value 
//=== checks value as well as data type too
let x=" "//empty string is false
let y=false
let data=x==y
console.log(data)